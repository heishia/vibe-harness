---
description: 하네스에서 하지 말 것. 새 프로젝트 기본값으로 넣지 않는 패턴.
alwaysApply: true
---

# 하지 말 것

- 캐시 없을 때 메모리 폴백을 운영 기본으로
- 클라 헤더만으로 앱 전용 API 인가
- 공급사를 한 파일에서 계속 분기
- 긴 폴링을 비동기 기본으로
- 관리자를 SSR 껍데기 SPA로 강제
- 분류를 클라 정규식으로 추론
- 공유 타입/UI를 영원히 금지
- 웹뷰 여백 매직넘버를 다른 앱에 복사
- 모든 상품에 4층 주문·확정 후 이행을 강제
- 모든 앱을 Capacitor로 강제
- 로그인 카트를 로컬만으로
- 클라 경로만으로 권한
- 손수 sitemap을 진실의 원천으로
- 프로세스 메모리 타이머로 재시도
- 웹훅을 IP만으로 막기
- 마켓을 웹훅 없이 폴링만
- 시트를 런타임 카탈로그로
- Play 내부 트랙을 기본 업로드로
- AAB만 만들고 업로드 생략
- 로컬 Xcode 없다고 iOS 배포를 중단
- 코딩 하네스에 비개발자 보고 형식을 넣기
- 채팅/docs 이미지를 앱이 직접 import
- 디자인툴 원본(.fig)을 `public`에 넣기
- inbox를 배포 경로로 쓰기
- 같은 에셋을 public·docs·버킷에 복제
- 브랜드명·특정 벤더명을 규칙으로 박기
- 비밀값을 커밋하거나 채팅에 다시 적기
